# Finance RAG Notebook requirements (Python 3.11 / 3.12 recommended)
# One Chroma DB per embedding, per-chunk-size collections; tqdm progress bars.
# Note: tiktoken is intentionally omitted to avoid Rust build on some Python versions.

# --- Core RAG stack ---
langchain>=0.3.7,<0.4.0
langchain-community>=0.3.7,<0.4.0
langchain-openai>=0.2.0,<0.3.0

llama-index==0.11.18
llama-index-embeddings-huggingface>=0.3.0,<0.4.0
llama-index-embeddings-openai>=0.2.0,<0.4.0
llama-index-embeddings-ollama>=0.2.0,<0.4.0

chromadb>=0.5.16,<0.6.0

# --- Embedding backends ---
sentence-transformers>=3.1.1,<3.2.0
openai>=1.51.0,<2.0.0
ollama>=0.3.0,<0.4.0   # optional; install if you use Ollama embeddings

# --- Utilities ---
ipykernel
pip install ipywidgets>=8.1
tqdm>=4.66,<5.0
python-dotenv>=1.0,<2.0
pydantic>=2.7,<3.0
numpy>=1.26,<3.0

# --- Optional (Notebook runtime) ---
# jupyterlab>=4.2,<5.0
# ipykernel>=6.29,<7.0
